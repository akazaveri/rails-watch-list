<div class="container mt-5">
  <h2>Create a new list</h2>
  <div>
    <%= simple_form_for @list do |f| %>
      <%= f.input :name %>
      <%= f.input :photo, as: :file, label: "Add an image for your list" %>
      <%= f.submit class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
